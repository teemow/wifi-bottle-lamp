tube_diameter = 43.5;
adapter_length = 8;
adapter_thickness = 1;
ring_length = 1;
ring_thickness = 2;

resolution = 100;

use <lib.scad>;

middle(tube_diameter, adapter_length, adapter_thickness, ring_length, ring_thickness, resolution);